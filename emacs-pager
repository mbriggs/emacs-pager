#!/bin/sh

# Based on https://github.com/mbriggs/emacs-pager/pull/2

tdir="/tmp/emacs-pager-$USER"
mkdir -p "$tdir"
t=$(mktemp -p "$tdir" --suffix .emacs-pager) || exit 1
cat - >> $t
echo 'reading into emacs...'
trap "rm -f -- $t" EXIT
emacsclient --tty -a "" "$t"
