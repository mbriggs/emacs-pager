#!/usr/bin/env ruby

require 'digest/md5'

input = ARGF.read
file = "/tmp/#{Digest::MD5.hexdigest input}.emacs-pager"

File.open(file, 'w') do |f|
  f.write(input)
end

`emacsclient #{file}`

FileUtils.rm(file)
